{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "cds watch",
      "name": "harsh terminal",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}